#Desarrollar un algoritmo que escriba en pantalla, uno después del otro, 
#los resultados de lafunción n^x desde 0 hasta n, con n entero positivo máximo 9 y x positivo máximo de 9

base = int(input("ingres el numero que corresponde a la base de la potencia:  "))
exponente = int(input("ingrese la potencia a la que desea elevar su base: "))
cont = 0
if 0 < exponente <= 9 and 0 <= base <=9 :
    while True:
        if cont <= base:
            print(cont**exponente)
            cont += 1
        else:
            break
else:
    print("la base debe ser un numero entre 0 y 9, y el exponente tiene que ser un numero entero positivo hasta 9")
